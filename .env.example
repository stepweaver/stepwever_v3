# Copy to `.env.local` and set real values. This file is safe to commit.

# Contact form (Nodemailer) — required for /api/contact
EMAIL_SERVICE=gmail
EMAIL_USER=
EMAIL_PASS=
EMAIL_TO=
SEND_CONFIRMATION_EMAIL=false
