/**
 * Optional dynamic room copy hooks. Static descriptions live in world/rooms.ts.
 * Use this module when room text depends on flags or item state.
 */
export const ROOM_TEXT_NOTE =
  'Extend with functions keyed by room id when puzzles need bespoke prose.';
