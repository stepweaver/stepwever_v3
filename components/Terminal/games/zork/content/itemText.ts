/**
 * Long-form or conditional item prose. Base strings live on ItemDef in world/items.ts.
 */
export const ITEM_TEXT_NOTE =
  'Add formatItemDescription(state, id) here when an item needs stateful copy.';
