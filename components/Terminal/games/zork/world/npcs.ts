/**
 * NPC catalog placeholder; combat/attack resolves against this when populated.
 * Phase 1 leaves this empty; attack/kill returns a generic fallback.
 */
export const NPCS: Record<string, { id: string; name: string }> = {};
