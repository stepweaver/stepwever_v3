/**
 * Hook for puzzle-specific behavior that does not fit open/move/inventory.
 * Window and trap-door scripting lives in containers.ts for now.
 */
export const SPECIAL_CASES_NOTE = 'Add dispatch hooks here as the world grows.';
